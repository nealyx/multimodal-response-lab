# ── Core ML framework ─────────────────────────────────────────────────────────
# MPS (Apple Silicon GPU) support stabilized in 2.0; require it as the floor.
# Install the correct torch build for your platform via the instructions in
# README.md — do NOT install the PyPI wheel on macOS with CUDA flags.
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ── Config management ─────────────────────────────────────────────────────────
PyYAML>=6.0

# ── Computer vision / capture ─────────────────────────────────────────────────
opencv-python>=4.8.0
Pillow>=10.0.0
# mediapipe ships its own bundled native libs; no separate protobuf pin needed.
# 0.10+ uses the new Task API but still exposes the legacy solutions namespace.
mediapipe>=0.10.0

# ── Numerical computing ───────────────────────────────────────────────────────
numpy>=1.24.0
scikit-learn>=1.3.0   # PCA, KMeans, IsolationForest (Day 10 embeddings)
matplotlib>=3.7.0     # charts and embedding plots (Days 8, 10)

# ── Progress / CLI ────────────────────────────────────────────────────────────
tqdm>=4.65.0
rich>=13.0.0          # structured console output for dev/debug runs

# ── Multimodal models (Hugging Face ecosystem) ────────────────────────────────
transformers>=4.35.0
accelerate>=0.24.0    # device placement & mixed-precision helpers

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
